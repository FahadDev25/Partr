<div class="w-full">
  <%= render partial: "pages/notice", locals: { modal: false } %>

  <% content_for :title, "Team roles" %>

  <div class="flex justify-between items-center mb-2">
    <h1 class="font-bold text-4xl">Team roles</h1>
    <%= link_to "New team role", new_team_role_path, class: "button primary" %>
  </div>

  <%= render partial: "team_role_table" %>
</div>
