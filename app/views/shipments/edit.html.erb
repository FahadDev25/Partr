<%= modal(header_divider: false, footer_divider: false) do |m| %>
  <% m.title do %>
    <h1>Editing Shipment</h1>
  <% end %>
  <div class="mx-auto w-full">
    <%= render "form", team: @team, shipment: @shipment %>
  </div>
<% end %>
