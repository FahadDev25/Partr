<%= modal(header_divider: false, footer_divider: false) do |m| %>
  <% m.title do %>
    <h1>Editing Job</h1>
  <% end %>
  <div class="mx-auto md:w-2/3 w-full">
    <%= render "form", team: @team, job: @job %>

    <%= link_to "Show This Job", @job, class: "button show-button" %>
  </div>
<% end %>
