<%= form_with(model: comment, class: "contents") do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :commentable_id, value: comment.commentable_id || id %>
  <%= form.hidden_field :commentable_type, value: comment.commentable_type || type %>

<p class='text-dd-black font-bold text-lg mb-1'>Your Notes:</p>
  <p class='dd-shipment-detail-comment-input'><%= form.rich_text_area :content , placeholder: "Type Here"%></p>

  <div class="inline dd-create-comment-btn">
    <%= form.submit class: "-mt-2 rounded-b-md py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>

<style>
.dd-shipment-detail-comment-input .trix-content {
  background-color: #4F807B0D;
  border-top: 1px solid #4F807B;
  border-left: 1px solid #4F807B;
  border-right: 1px solid #4F807B;
  border-bottom: 0;
  border-top-left-radius: 7px;
  border-top-right-radius: 7px;
  min-height: 8rem;
}

.dd-create-comment-btn input{
 border-bottom-left-radius: 7px;
 border-bottom-right-radius: 7px;
 margin-top: -3px;
}
</style>