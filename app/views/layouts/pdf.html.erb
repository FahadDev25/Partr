<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://use.typekit.net/nqu7dva.css">
    <%= stylesheet_link_tag "tailwind", "inter-font" %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
    <style>
      html {
        -webkit-print-color-adjust: exact;
        background-color: white;
        height: auto;
      }
      body {
        font-family: Arial, sans-serif;
        height: auto;
      }
      h1 {
        font-size: 50px;
        color: #b21212;
        margin-top: 5px;
        margin-bottom: 5px;
      }
      h2 {
        font-size: 20px;
        color: #b21212;
        margin-top: 5px;
        margin-bottom: 5px;
      }
      p {
        font-size: 12px;
        margin-top: 3px;
        margin-bottom: 3px;
      }
      span {
        font-size: 12px;
        margin-top: 5px;
        margin-bottom: 5px;
      }
      hr {
        border-top: 3px solid #5b4381;
        margin-top: 10px;
        margin-bottom: 10px;
      }
      table {
        width: 100%;
        margin-top: 10px;
        border-collapse: collapse;
      }
      th {
        border-bottom: 4px solid #5b4381;
        color: #5b4381;
        padding: 2px;
        font-size: 12px;
        text-align: left;
        vertical-align: bottom;
      }
      td {
        border: 2px solid #5b4381;
        padding: 5px;
        font-size: 12px;
        text-align: right;
      }
      .grid-container {
        display: grid;
        grid-template-columns: 1fr 1fr;
      }
      .grid-item {
        padding: 10px;
        border: 1px solid rgba(0, 0, 0, 0.8);
        margin-bottom: 10px;
        margin-left: 5px;
        margin-right: 5px;
      }
      .pagebreak { 
        page-break-before: always; 
      }
      .two-line-wrap {
        font-size: 14px;
        text-wrap: wrap;
        text-overflow: ellipsis;
        overflow: hidden;
        max-height: 32px;
        line-height: 16px;
        display: -webkit-box; 
        -webkit-line-clamp: 2; 
        -webkit-box-orient: vertical;
      }
      .centered-img {
        display: block;
        margin-left: auto;
        margin-right: auto;
      }
      main {
        position: relative;
      }
      footer {
        bottom: 0;
        left:0;
        right: 0;
        width: 100%;
        position: fixed;
      }
      .qr-content {
        display: flex;
        justify-content: space-between;
      }
      .qr-label {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
        margin-top: auto;
        margin-bottom: auto;
      }
    </style>
  </head>
    <body>
      <%= yield %>
    </body>
</html>